let velocidadePermitida = 80;
let velocidadeDoCarro = 50;

if (velocidadeDoCarro > velocidadePermitida) {
    console.log(`Esta acima de ${velocidadePermitida}`);
}else{
    console.log(`Esta dentro da velocidade permitida - Velocidade: ${velocidadeDoCarro}`);
}